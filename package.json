{
  "name": "banking-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "yarn run clean && tsc && tsc-alias && cp -r src/graphql/schema/typeDefs dist/graphql/schema/typeDefs",
    "dev": "yarn run compile && node ./dist/server.js",
    "dev:watch": "nodemon -e ts,graphql --ignore dist/ --exec \"yarn run dev\""
  },
  "keywords": [],
  "author": "Patrick T",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.0.0",
    "@graphql-tools/load-files": "^7.0.0",
    "@graphql-tools/merge": "^9.0.0",
    "@graphql-tools/mock": "^8.7.6",
    "@graphql-tools/schema": "^9.0.4",
    "@graphql-tools/utils": "^10.0.8",
    "graphql": "^16.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "tsc-alias": "^1.8.8",
    "typescript": "^5.2.2"
  }
}
